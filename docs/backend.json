{
  "entities": {
    "User": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "User",
      "type": "object",
      "description": "Represents a user of the Desi Content Creator application.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the user (matches auth.uid)."
        },
        "name": {
          "type": "string",
          "description": "User's display name."
        },
        "photo": {
          "type": "string",
          "description": "URL of the user's profile photo.",
          "format": "uri"
        },
        "plan": {
          "type": "string",
          "description": "The user's subscription plan."
        }
      },
      "required": [
        "id"
      ]
    },
    "History": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "History",
      "type": "object",
      "description": "Represents a single analysis history record for a user.",
      "properties": {
        "userId": {
          "type": "string",
          "description": "The user who initiated the analysis."
        },
        "mode": {
          "type": "string",
          "description": "The task mode used for the analysis (e.g., 'analyze', 'improve')."
        },
        "content": {
          "type": "string",
          "description": "The user's original content/prompt for analysis."
        },
        "result": {
          "type": "object",
          "description": "The structured JSON result from the AI analysis."
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp indicating when the analysis was performed.",
          "format": "date-time"
        }
      },
      "required": [
        "userId",
        "mode",
        "content",
        "result",
        "createdAt"
      ]
    }
  },
  "auth": {
    "providers": [
      "password",
      "anonymous"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/users/{userId}",
        "definition": {
          "schema": {
            "$ref": "#/entities/User"
          },
          "description": "Stores user profile information."
        }
      },
      {
        "path": "/history/{historyId}",
        "definition": {
          "schema": {
            "$ref": "#/entities/History"
          },
          "description": "Stores analysis history for all users."
        }
      }
    ]
  }
}
